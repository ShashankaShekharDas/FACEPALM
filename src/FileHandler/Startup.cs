namespace FileHandler
{
    /// <summary>
    /// Entry point for FileHandler executable. Currently not intended to be invoked directly.
    /// </summary>
    internal static class Startup
    {
        public static void Main()
        {
            // This project only exposes library functionality. Starting it directly is not supported.
            throw new NotSupportedException("FileHandler is a class library and cannot be started as an application.");
        }
    }
}